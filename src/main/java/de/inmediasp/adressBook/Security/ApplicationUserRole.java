/*
package de.inmediasp.adressBook.Security;
import com.google.common.collect.Sets;
import java.util.Set;
import static  de.inmediasp.adressBook.Security.ApplicationUserPermision.*;

public enum ApplicationUserRole {
    USER(Sets.newHashSet()),
    ADMIN(Sets.newHashSet(USER_READ,
            USER_WRITE));
    private final Set<ApplicationUserPermision> permissions;

    ApplicationUserRole(Set<ApplicationUserPermision> permissions) {
        this.permissions = permissions;
    }

}
*/
